export namespace Icon {
	export enum file {
		blank = 0,
		text,
		script,
		image,
	}

	export enum folder {
		empty = 8,
		full,
	}

	export enum application {
		file_manager = 16,
		minesweeper,
		terminal,
		settings,
	}
}
