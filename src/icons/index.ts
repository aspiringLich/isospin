export namespace Icon {
	export enum file {
		blank,
		text,
	}

	export enum folder {
		empty = 8,
		full,
	}

	export enum application {
		file_manager = 16,
		minesweeper,
		terminal,
		settings,
	}
}
