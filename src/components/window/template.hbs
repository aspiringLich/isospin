// This file is automatically generated by `gen_registry.py`

import { Registry, type Item } from "$cpt/window";
export const registry = new Registry();

{{#each templates}}
import {{id}} from "./{{file}}.svelte";
{{/each}}

{{#each registry}}
{{#if file}}
import {{id}} from "./{{file}}.svelte";
registry.register_window("{{id}}", {{id}}, {{{props}}});
{{else}}
registry.register_window("{{id}}", {{base}}, {{{props}}});
{{/if}}
{{#if app}}
registry.register_app("{{id}}", "{{title}}", "{{app.icon}}", {{app.row}});
{{/if}}
{{/each}}